# M336 Search Guides

Issue-numbered directories contain non-scoring exact-legal placements or
explicit collision-relaxed hybrids used only to generate candidate sites.
Each directory includes a manifest with content hashes and the source
incumbent. A certified search guide must never replace the scoring checkpoint
unless it independently improves the accepted objective and receives a new
checkpoint export.
